<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.interoberlin.lymbo" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="de.interoberlin.lymbo.view.activities.HomeActivity"
            android:label="@string/app_name"
            android:parentActivityName="de.interoberlin.lymbo.SplashActivity">
        </activity>
        <activity
            android:name="de.interoberlin.lymbo.view.activities.DetailActivity"
            android:label="@string/app_name"
            android:parentActivityName="de.interoberlin.lymbo.SplashActivity">
        </activity>
    </application>

</manifest>
